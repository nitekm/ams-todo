spring:
  application:
    name: todo-service
server:
  port: 0
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8762/eureka}
  instance:
    prefer-ip-address: true
grpc:
  server:
    port: 9090
